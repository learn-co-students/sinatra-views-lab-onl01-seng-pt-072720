<!DOCTYPE html>
<html>
    <head>
        <title>Hey There</title>
    </head>
    <body>
      <% today = DateTime.now.strftime("The date is %A, %B %d, %Y") %>
  <p><%= today %></p>
    </body>
</html>
